# Linear Regression Demo

This project lives in its own repository, not in this portfolio repo:

https://github.com/Cgiles8773/Machine-Learning

The live demo (`web/` in that repo) is deployed as its own standalone Vercel
project and linked into this portfolio via the `liveUrl` field in
[`src/data/projects.js`](../../src/data/projects.js) — see the `hubble` entry
for the same pattern. No code is duplicated here.
